fragment_downloaded_cb({"url": "gtkclipboard-h.html#gtk_clipboard_wait_for_text", "fragment": "Gtk.Clipboard.WaitForText \nRequests the contents of the clipboard as text and converts the result to UTF if necessary. This function waits for the data to be received using the main loop so events timeouts etc may be dispatched during the wait. \na newly allocated UTF string which must be freed with GLib.Global.Free or null if retrieving the selection data failed. This could happen for various reasons in particular if the clipboard was empty or if the contents of the clipboard could not be converted into text form. \n"});