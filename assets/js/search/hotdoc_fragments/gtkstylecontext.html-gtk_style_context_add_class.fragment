fragment_downloaded_cb({"url": "gtkstylecontext.html#gtk_style_context_add_class", "fragment": "gtk_style_context_add_class \nGtk.StyleContext.add_class \nGtk.prototype.add_class \nAdds a style class to context so posterior calls to gtk_style_context_get or any of the gtk_render_ functions will make use of this new class for styling. \nIn the CSS file format a GtkEntry defining a search class would be matched by \nWhile any widget defining a search class would be matched by \nParameters \na GtkStyleContext \nclass name to use in styling \nSince \nAdds a style class to context so posterior calls to gtk_style_context_get not introspectable or any of the gtk_render_ functions will make use of this new class for styling. \nIn the CSS file format a Gtk.Entry defining a search class would be matched by \nWhile any widget defining a search class would be matched by \nParameters \na Gtk.StyleContext \nclass name to use in styling \nSince \nAdds a style class to context so posterior calls to gtk_style_context_get not introspectable or any of the gtk_render_ functions will make use of this new class for styling. \nIn the CSS file format a Gtk.Entry defining a search class would be matched by \nWhile any widget defining a search class would be matched by \nParameters \na Gtk.StyleContext \nclass name to use in styling \nSince \n"});