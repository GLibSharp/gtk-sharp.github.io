fragment_downloaded_cb({"url": "gtkmain.html#gtk_main_quit", "fragment": "Gtk.main_quit \ngtk_main_quit \nprototype.main_quit \nMakes the innermost invocation of the main loop return when it regains control. \nMakes the innermost invocation of the main loop return when it regains control. \nMakes the innermost invocation of the main loop return when it regains control. \n"});