fragment_downloaded_cb({"url": "gtkfontbutton.html#GtkFontButton::font-set", "fragment": "font set \nfont set \nfont set \nThe font set signal is emitted when the user selects a font. When handling this signal use Gtk.prototype.get_font_name to find out which font was just selected. \nNote that this signal is only emitted when the user changes the font. If you need to react to programmatic font changes as well use the notify font name signal. \nParameters \nFlags Run First \nSince \nThe font set signal is emitted when the user selects a font. When handling this signal use Gtk.FontButton.get_font_name to find out which font was just selected. \nNote that this signal is only emitted when the user changes the font. If you need to react to programmatic font changes as well use the notify font name signal. \nParameters \nFlags Run First \nSince \nThe font set signal is emitted when the user selects a font. When handling this signal use gtk_font_button_get_font_name to find out which font was just selected. \nNote that this signal is only emitted when the user changes the font. If you need to react to programmatic font changes as well use the notify font name signal. \nParameters \nFlags Run First \nSince \n"});