fragment_downloaded_cb({"url": "gtkwidget.html#gtk_widget_show_now", "fragment": "gtk_widget_show_now \nGtk.Widget.show_now \nGtk.prototype.show_now \nShows a widget. If the widget is an unmapped toplevel widget i.e. a GtkWindow that has not yet been shown enter the main loop and wait for the window to actually be mapped. Be careful because the main loop is running anything can happen during this function. \nParameters \na GtkWidget \nShows a widget. If the widget is an unmapped toplevel widget i.e. a Gtk.Window that has not yet been shown enter the main loop and wait for the window to actually be mapped. Be careful because the main loop is running anything can happen during this function. \nParameters \na Gtk.Widget \nShows a widget. If the widget is an unmapped toplevel widget i.e. a Gtk.Window that has not yet been shown enter the main loop and wait for the window to actually be mapped. Be careful because the main loop is running anything can happen during this function. \nParameters \na Gtk.Widget \n"});