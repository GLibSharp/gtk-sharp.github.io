fragment_downloaded_cb({"url": "gtkflowbox-h.html#gtk_flow_box_set_filter_func", "fragment": "gtk_flow_box_set_filter_func \nGtk.prototype.set_filter_func \nGtk.FlowBox.set_filter_func \nBy setting a filter function on the box one can decide dynamically which of the children to show. For instance to implement a search function that only shows the children matching the search terms. \nThe filter_func will be called for each child after the call and it will continue to be called each time a child changes via gtk_flow_box_child_changed or when gtk_flow_box_invalidate_filter is called. \nNote that using a filter function is incompatible with using a model see gtk_flow_box_bind_model \nParameters \na GtkFlowBox \ncallback that lets you filter which children to show \nuser data passed to filter_func \ndestroy notifier for user_data \nSince \nBy setting a filter function on the box one can decide dynamically which of the children to show. For instance to implement a search function that only shows the children matching the search terms. \nThe filter_func will be called for each child after the call and it will continue to be called each time a child changes via Gtk.prototype.changed or when Gtk.prototype.invalidate_filter is called. \nNote that using a filter function is incompatible with using a model see Gtk.prototype.bind_model \nParameters \na Gtk.FlowBox \ncallback that lets you filter which children to show \nuser data passed to filter_func \ndestroy notifier for user_data \nSince \nBy setting a filter function on the box one can decide dynamically which of the children to show. For instance to implement a search function that only shows the children matching the search terms. \nThe filter_func will be called for each child after the call and it will continue to be called each time a child changes via Gtk.FlowBoxChild.changed or when Gtk.FlowBox.invalidate_filter is called. \nNote that using a filter function is incompatible with using a model see Gtk.FlowBox.bind_model \nParameters \na Gtk.FlowBox \ncallback that lets you filter which children to show \nuser data passed to filter_func \ndestroy notifier for user_data \nSince \n"});