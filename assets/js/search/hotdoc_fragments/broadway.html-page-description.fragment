fragment_downloaded_cb({"url": "broadway.html#page-description", "fragment": "The GDK Broadway backend provides support for displaying GTK applications in a web browser using HTML5 and web sockets. To run your application in this way select the Broadway backend by setting GDK_BACKEND broadway. Then you can make your application appear in a web browser by pointing it at http Note that you need to enable web sockets in your web browser. \nYou can choose a different port from the default by setting the BROADWAY_DISPLAY environment variable to the port that you want to use. \nIt is also possible to use multiple GTK applications in the same web browser window by using the Broadway server broadwayd that ships with GTK To use broadwayd start it like this \nThen point your web browser at http Start your applications like this \nBROADWAY_DISPLAY. \nSpecifies the Broadway display number. The default display is The display number determines the port to use when connecting to a Broadway application via the following formula \n"});