fragment_downloaded_cb({"url": "gtkplacessidebar-h.html#gtk_places_sidebar_get_nth_bookmark", "fragment": "Gtk.PlacesSidebar.GetNthBookmark \nThis function queries the bookmarks added by the user to the places sidebar and returns one of them. This function is used by Gtk.IFileChooser to implement the Alt Alt etc. shortcuts which activate the cooresponding bookmark. \nParameters \nindex of the bookmark to query \nThe bookmark specified by the index n or null if no such index exist. Note that the indices start at even though the file chooser starts them with the keyboard shortcut Alt \nSince \n"});