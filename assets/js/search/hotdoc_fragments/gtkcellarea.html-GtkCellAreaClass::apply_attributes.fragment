fragment_downloaded_cb({"url": "gtkcellarea.html#GtkCellAreaClass::apply_attributes", "fragment": "apply_attributes \napply_attributes \napply_attributes \nApply the cell attributes to the cells. This is implemented as a signal and generally Gtk.CellArea subclasses don t need to implement it since it is handled by the base class. \nParameters \nApply the cell attributes to the cells. This is implemented as a signal and generally Gtk.CellArea subclasses don t need to implement it since it is handled by the base class. \nParameters \nApply the cell attributes to the cells. This is implemented as a signal and generally GtkCellArea subclasses don t need to implement it since it is handled by the base class. \nParameters \n"});