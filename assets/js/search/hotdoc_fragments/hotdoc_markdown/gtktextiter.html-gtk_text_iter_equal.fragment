fragment_downloaded_cb({"url": "hotdoc_markdown/gtktextiter.html#gtk_text_iter_equal", "fragment": "gtk_text_iter_equal \nGtk.prototype.equal \nGtk.TextIter.equal \nTests whether two iterators are equal using the fastest possible mechanism. This function is very fast you can expect it to perform better than e.g. getting the character offset for each iterator and comparing the offsets yourself. Also it s a bit faster than gtk_text_iter_compare. \nParameters \na GtkTextIter \nanother GtkTextIter \nTRUE if the iterators point to the same place in the buffer \nTests whether two iterators are equal using the fastest possible mechanism. This function is very fast you can expect it to perform better than e.g. getting the character offset for each iterator and comparing the offsets yourself. Also it s a bit faster than Gtk.prototype.compare. \nParameters \na Gtk.TextIter \nanother Gtk.TextIter \ntrue if the iterators point to the same place in the buffer \nTests whether two iterators are equal using the fastest possible mechanism. This function is very fast you can expect it to perform better than e.g. getting the character offset for each iterator and comparing the offsets yourself. Also it s a bit faster than Gtk.TextIter.compare. \nParameters \na Gtk.TextIter \nanother Gtk.TextIter \nTrue if the iterators point to the same place in the buffer \n"});