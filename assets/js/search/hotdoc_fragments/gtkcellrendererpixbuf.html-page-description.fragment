fragment_downloaded_cb({"url": "gtkcellrendererpixbuf.html#page-description", "fragment": "A GtkCellRendererPixbuf can be used to render an image in a cell. It allows to render either a given GdkPixbuf set via the pixbuf property or a named icon set via the icon name property \nTo support the tree view GtkCellRendererPixbuf also supports rendering two alternative pixbufs when the self.props.is_expander property is TRUE. If the self.props.is_expanded property is TRUE and the pixbuf expander open property is set to a pixbuf it renders that pixbuf if the self.props.is_expanded property is FALSE and the pixbuf expander closed property is set to a pixbuf it renders that one. \n"});