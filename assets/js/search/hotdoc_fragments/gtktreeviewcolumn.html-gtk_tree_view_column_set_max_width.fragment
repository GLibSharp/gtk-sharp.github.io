fragment_downloaded_cb({"url": "gtktreeviewcolumn.html#gtk_tree_view_column_set_max_width", "fragment": "Gtk.TreeViewColumn.set_max_width \ngtk_tree_view_column_set_max_width \nGtk.prototype.set_max_width \nSets the maximum width of the tree_column. If max_width is then the maximum width is unset. Note the column can actually be wider than max width if it s the last column in a view. In this case the column expands to fill any extra space. \nParameters \nA Gtk.TreeViewColumn. \nThe maximum width of the column in pixels or \nSets the maximum width of the tree_column. If max_width is then the maximum width is unset. Note the column can actually be wider than max width if it s the last column in a view. In this case the column expands to fill any extra space. \nParameters \nA GtkTreeViewColumn. \nThe maximum width of the column in pixels or \nSets the maximum width of the tree_column. If max_width is then the maximum width is unset. Note the column can actually be wider than max width if it s the last column in a view. In this case the column expands to fill any extra space. \nParameters \nA Gtk.TreeViewColumn. \nThe maximum width of the column in pixels or \n"});