fragment_downloaded_cb({"url": "gtkmain.html#gtk_main", "fragment": "Gtk.main \ngtk_main \nprototype.main \nRuns the main loop until Gtk.main_quit is called. \nYou can nest calls to Gtk.main. In that case Gtk.main_quit will make the innermost invocation of the main loop return. \nRuns the main loop until gtk_main_quit is called. \nYou can nest calls to gtk_main. In that case gtk_main_quit will make the innermost invocation of the main loop return. \nRuns the main loop until prototype.main_quit is called. \nYou can nest calls to prototype.main. In that case prototype.main_quit will make the innermost invocation of the main loop return. \n"});