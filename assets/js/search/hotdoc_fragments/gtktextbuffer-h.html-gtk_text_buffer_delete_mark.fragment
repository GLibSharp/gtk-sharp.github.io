fragment_downloaded_cb({"url": "gtktextbuffer-h.html#gtk_text_buffer_delete_mark", "fragment": "Gtk.TextBuffer.DeleteMark \nDeletes mark so that it s no longer located anywhere in the buffer. Removes the reference the buffer holds to the mark so if you haven t called GObject.Object.Ref on the mark it will be freed. Even if the mark isn t freed most operations on mark become invalid until it gets added to a buffer again with Gtk.TextBuffer.AddMark. Use Gtk.TextMark.GetDeleted to find out if a mark has been removed from its buffer. The MarkDeleted signal will be emitted as notification after the mark is deleted. \nParameters \na Gtk.TextMark in buffer \n"});