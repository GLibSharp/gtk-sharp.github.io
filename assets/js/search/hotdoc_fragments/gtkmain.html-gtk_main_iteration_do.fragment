fragment_downloaded_cb({"url": "gtkmain.html#gtk_main_iteration_do", "fragment": "Gtk.main_iteration_do \ngtk_main_iteration_do \nprototype.main_iteration_do \nRuns a single iteration of the mainloop. If no events are available either return or block depending on the value of blocking. \nParameters \nTrue if you want GTK to block if no events are pending \nTrue if Gtk.main_quit has been called for the innermost mainloop \nRuns a single iteration of the mainloop. If no events are available either return or block depending on the value of blocking. \nParameters \nTRUE if you want GTK to block if no events are pending \nTRUE if gtk_main_quit has been called for the innermost mainloop \nRuns a single iteration of the mainloop. If no events are available either return or block depending on the value of blocking. \nParameters \ntrue if you want GTK to block if no events are pending \ntrue if prototype.main_quit has been called for the innermost mainloop \n"});