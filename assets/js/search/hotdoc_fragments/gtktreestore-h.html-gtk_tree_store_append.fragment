fragment_downloaded_cb({"url": "gtktreestore-h.html#gtk_tree_store_append", "fragment": "Gtk.TreeStore.Append \nAppends a new row to tree_store. If parent is non NULL then it will append the new row after the last child of parent otherwise it will append a row to the top level. iter will be changed to point to this new row. The row will be empty after this function is called. To fill in values you need to call gtk_tree_store_set not introspectable or Gtk.TreeStore.SetValue. \nParameters \nAn unset Gtk.TreeIter to set to the appended row \nA valid Gtk.TreeIter or null \n"});